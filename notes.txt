TODO:
    Clean up unnecessary code
        Remove JWT Code.
        Check for other unnecessary code also.
    Dockerize the application and add docker compose with both db and api.
    Add script to connect to dev db

docker run --name postgres-container -e POSTGRES_PASSWORD=LpAe7djAicW2f634kRr5 -d -p 5432:5432 postgres;
docker exec -it postgres-container psql -U postgres;
CREATE USER "synapse-db-admin" WITH  PASSWORD 'LpAe7djAicW2f634kRr5' CREATEDB;
CREATE DATABASE "synapse-db";
GRANT USAGE on schema public to "synapse-db-admin";
GRANT ALL PRIVILEGES on DATABASE "synapse-db" to "synapse-db-admin";
postgres=# \c "synapse-db" postgres
GRANT ALL ON SCHEMA public TO "synapse-db-admin";

docker start postgres-container && docker logs -f postgres-container
docker exec -it postgres-container psql -U synapse-db-admin -d synapse-db
